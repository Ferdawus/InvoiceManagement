import Vue from "vue";
import Vuetify from "vuetify/lib/framework";

Vue.use(Vuetify);

export default new Vuetify({
    // Vuetify configuration options go here
    theme: {
        // Define your theme options
    },
});
